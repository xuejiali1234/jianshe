Runtime and offline datasets for traffic prediction.

- `raw/`: realtime 60-second SUMO aggregations and batch simulation outputs.
- `processed/`: cleaned long tables.
- `datasets/`: sliding-window samples for model training.
